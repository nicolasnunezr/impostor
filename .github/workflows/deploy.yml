name: Deploy to Render

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Render
        run: |
          echo "Push your code to GitHub and Render will auto-deploy"
          echo "Or use Railway.app which auto-detects Flask apps"

